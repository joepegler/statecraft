---
title: Isolation Fixtures
description: Isolate side effects inside a scenario by snapshotting and reverting the runtime with withSnapshot.
---

# Isolation Fixtures

**Test runners:** Examples use Vitest. `scenario(...)` works with any runner that accepts an async test callback (Jest, Node `node:test`, and others).

Isolation fixtures help you keep tests independent even when you reuse a runtime across multiple scenarios.

Use `withSnapshot()` when a scenario mutates state but your suite reuses a runtime handle and should not leak side effects into later tests.

## Fixtures

- [`withSnapshot()`](/fixtures/isolation/withSnapshot)

