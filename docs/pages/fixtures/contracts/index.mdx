---
title: Contracts & Deployment Fixtures
description: Inject runtime bytecode at fixed addresses with withContracts, or deploy with constructor semantics via withDeployments.
---

# Contracts & Deployment Fixtures

**Test runners:** Examples use Vitest. `scenario(...)` works with any runner that accepts an async test callback (Jest, Node `node:test`, and others).

These fixtures set up contract code at deterministic addresses on your scenario runtime.

## Fixtures

- [`withContracts({ ... })` (bytecode injection)](/fixtures/contracts/withContracts)
- [`withDeployments({ ... })` (constructor-real deployment)](/fixtures/contracts/withDeployments)

