diff --git a/_lib/app/index.server.js b/_lib/app/index.server.js
--- a/_lib/app/index.server.js
+++ b/_lib/app/index.server.js
@@ -7,6 +7,14 @@
 import { routes } from './routes.js';
 import { createFetchRequest } from './utils/createFetchRequest.js';
 export async function prerender(location) {
+    const { config } = await resolveVocsConfig();
+    const { basePath } = config;
+    const normalizedBase = basePath.replace(/\/$/, '');
+    const locationForRouter = normalizedBase === '' || basePath === '/'
+        ? location
+        : location === '/'
+            ? `${normalizedBase}/`
+            : `${normalizedBase}${location.startsWith('/') ? location : `/${location}`}`;
     const unwrappedRoutes = (await Promise.all(routes.map(async (route) => {
         const location_ = location === '/' ? '/' : location.replace(/\/$/, '');
         const path = route.path.replace(/\.html$/, '');
@@ -18,9 +26,7 @@
             element,
         };
     }))).filter(Boolean);
-    const { config } = await resolveVocsConfig();
-    const { basePath } = config;
-    return (_jsx(ConfigProvider, { config: config, children: _jsx(StaticRouter, { location: location, basename: basePath, children: _jsx(Routes, { children: unwrappedRoutes.map((route) => (_jsx(Route, { path: route.path, element: route.element }, route.path))) }) }) }));
+    return (_jsx(ConfigProvider, { config: config, children: _jsx(StaticRouter, { location: locationForRouter, basename: basePath, children: _jsx(Routes, { children: unwrappedRoutes.map((route) => (_jsx(Route, { path: route.path, element: route.element }, route.path))) }) }) }));
 }
 export async function render(req) {
     const { config } = await resolveVocsConfig();
